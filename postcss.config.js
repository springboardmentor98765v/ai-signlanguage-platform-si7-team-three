export default {
  plugins: {
    // Tailwind v4 is handled via the Vite plugin (@tailwindcss/vite)
    // Keep PostCSS empty to avoid trying to use tailwindcss as a PostCSS plugin.
    autoprefixer: {},
  },
};


